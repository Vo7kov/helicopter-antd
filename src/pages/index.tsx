import React from "react";
import { Button } from "antd";

const Home: React.FC = () => (
  <div className="App">
    <Button className="bg-purple-600" type="primary">
      Button
    </Button>
  </div>
);

export default Home;
